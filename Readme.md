1. install node 
2. Install playwright 
3. Install type defination
 npm i --save-dev @types/node
 npm install --save-dev typescript
 npx playwright test --update-snapshots
3. Run the test 
npx playwright test